package jms.broker.main;

public class ActiveMqBrokerSSL {

    // 使用带SSL安全的BrokerService对象
    // SslBrokerService broker = new SslBrokerService();
    // SslContext sslContext = new SslContext();
    // sslContext.setTrustManagers(Arrays.asList(readTruststore(messageQueueProperties)));
    // sslContext.setKeyManagers(Arrays.asList(readKeystore(messageQueueProperties)));
    // broker.setSslContext(sslContext);
}
